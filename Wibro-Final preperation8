PRINT THE SUM OF MAX AND MIN VALUES IN GIVEN LIST

list=[9,8,6,3,2,10]
m=max(list)
list.remove(m)
n=max(list)
print(n+m)
  
  
 
CONVERT BINARY TO DECIMAL 
 
#include<stdio.h>
int main()
{
    int n;
    scanf("%d",&n);
    int r,sum=0,p=1;
    while(n != 0)
    {
    r=n%10;
    sum=sum+(r*p);
    p=p*2 ;
    n=n/10;
    }
    printf("%d",sum);
}
